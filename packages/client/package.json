{
  "name": "@decent/client",
  "version": "0.0.0",
  "description": "Standalone web client for Decent",
  "main": "index.js",
  "repository": "https://github.com/decent-chat/decent/tree/master/packages/client",
  "keywords": [
    "chat",
    "chat-system",
    "social",
    "decent"
  ],
  "author": "Multiple contributors",
  "license": "GPL-3.0",
  "dependencies": {
    "bel": "^5.1.5",
    "mrk.js": "^1.0.1",
    "nanobus": "^4.3.2",
    "nanochoo": "^6.6.3",
    "nanocomponent": "^6.5.0",
    "nanostate": "^1.0.3",
    "no-new": "^0.1.1"
  },
  "devDependencies": {
    "browserify": "^15.1.0",
    "css-extract": "^1.2.0",
    "dependency-check": "^2.10.0",
    "pretty-bytes-cli": "^2.0.0",
    "sheetify": "^6.2.0",
    "sheetify-nested": "^1.0.2",
    "snazzy": "^7.0.0",
    "standard": "^10.0.3",
    "tinyify": "^2.4.0",
    "watchify": "^3.9.0",
    "yo-yoify": "^4.3.0"
  },
  "scripts": {
    "build": "browserify src/app.js -o dist/bundle.js -g yo-yoify -t [ sheetify -u sheetify-nested ] -p [ css-extract -o dist/bundle.css ] -p tinyify && cat dist/bundle.js | gzip --best > dist/bundle.js.gz && cat dist/bundle.js.gz | wc -c | pretty-bytes",
    "watch": "watchify src/app.js -o dist/bundle.js -d -t [ sheetify -u sheetify-nested ] -p [ css-extract -o dist/bundle.css ] -v",
    "prepublishOnly": "npm run build",
    "test": "standard --verbose | snazzy && dependency-check . --entry src/app.js --missing --unused --no-dev -i sheetify"
  }
}
